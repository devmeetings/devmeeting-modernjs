{
  "name": "ts",
  "version": "0.0.1",
  "description": "Aplikacja Party List jako przykład kodu TypeScript",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch"
  },
  "devDependencies": {
    "typescript": "^1.7.5"
  },
  "dependencies": {
    "es6-promise": "^3.1.2"
  }
}
