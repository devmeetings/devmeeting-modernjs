{
  "name": "ts",
  "version": "0.0.1",
  "description": "Aplikacja Party List jako przykład kodu TypeScript",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch"
  },
  "devDependencies": {
    "typescript": "^1.7.5"
  }
}
